<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ '/style.css' | url }}">
    <title>{{ title }}</title>
</head>
<body>

  <div class="bg-green-400">
    <div class="container mx-auto flex flex-col items-left py-3 px-6 text-lg font-semibold text-white">
      <a href="{{ '/' | url }}">Whatsapp Form</a>
  </div>
  </div>
    <header class="flex flex-col items-center">
      <div class="text-2xl my-7">{{ title }}</div>
    </header>
    <main>
      {{ content | safe }}
    </main>
    <footer class="flex bg-green-400 pt-1 mt-8"><span></span></footer>
</body>
</html>