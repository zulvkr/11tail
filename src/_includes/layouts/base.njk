<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ '/style.css' | url }}">
    <title>{{ title }}</title>
</head>
<body>

  <div class="bg-green-400">
    <div class="container mx-auto flex flex-col items-left py-3 text-lg font-semibold text-white">
    Whatsapp Form
  </div>
  </div>
    <header>
      {{ title }}
    </header>
    <main>
      {{ content | safe }}
    </main>
</body>
</html>